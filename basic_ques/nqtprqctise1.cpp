#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;cin>>n;
    vector<double> arr(n);

    for(int i=0;i<n;i++)
        cin>>arr[i];
    for(int i=0;i<n;i++)
        cout<<arr[i]<<"\n";
    set<double> it;

 //   sort(arr.begin(),arr.end());
   // double p=*max_element(arr.begin(),arr.end());
    if(p==42.195)


    return 0;
}
