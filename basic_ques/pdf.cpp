#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;cin>>n;
    vector<int> h(n);
    for(int i=0;i<n;i++)
        cin>>h[i];
    vector<char> word;
    for(int i=0;i<word.size();i++)
        cin>>word[i];

    return 0;
}
