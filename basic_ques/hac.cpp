#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr;

    arr.push_back(0);
    arr.push_back(-2);
    arr.push_back(4);
    arr.push_back(3);
    arr.push_back(-1);
    arr.push_back(0);

    for(auto &x : arr)
        cout<< x <<" ";

    return 0;
}

